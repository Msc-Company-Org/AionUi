# Security Policy

- Never commit real tokens, passwords, API keys, private keys, or `.env` with real values.
- Keep examples in `.env.example` using safe placeholders (`*_EXAMPLE`).
- Do not include sensitive client/internal data in docs, logs or demo assets.
